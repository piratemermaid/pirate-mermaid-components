# Pirate Mermaid Components

## Usage

- Install in your project: `TODO`
- Use components: `TODO`

## Development

- Versions: node `22.15.0` and npm `10.9.2`
- Install dependencies: `npm install`
- Run storybook: `npm run storybook`
- Build storybook: `npm run build-storybook`
